listener 1883
# LAN-only broker inside the VM. TODO: add password_file auth before
# exposing beyond the VM or onboarding anything untrusted.
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
